fun main(){
    // 1.1
    //variables declaration
    var base = 10
    var height = 2
    var computation = "${(base*height) / 2}"

    println("The Area of the triangle is $computation")

    //1.2
    //variables declaration
    var a = 2
    var b = 1
    var computation2 = "${((a*2) + (2*a*b) - 4*(b+a)) / 2}"

    println("The value of c is $computation2")
}