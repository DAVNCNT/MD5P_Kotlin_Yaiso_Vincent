fun main(){
    //arithmetic operators
    println("This is an example of addition: ${2+5}")
    // store in a variable
    var sum: Int = 5+8
    var difference: Int = 10-5
    var product: Int = 8*5
    var quotient: Int = 100/25
    var modulo: Int = 5 % 2

    println("the sum is $sum")
    println("the difference is $difference")
    println("the product is $product")
    println("the quotient is $quotient")
    println("the modulo is $modulo")

    // 45 + 36 / 2 * 10
    var computation = (45+36)/(2*10)
    println("this is the result of the computation $computation")
}